import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // 필요한 CSS 파일을 import 합니다.
import App from './App';

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);
